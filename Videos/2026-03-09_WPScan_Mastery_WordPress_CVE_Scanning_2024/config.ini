# Configuration file for WPScan scripts
# API token should be set via environment variable WPSCAN_API_TOKEN for security.
# Do not store real tokens in this file.

[DEFAULT]

# Example usage in scripts: config.get('DEFAULT', 'api_token')
# But scripts prioritize environment variables for security.